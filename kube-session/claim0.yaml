apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: session-claim0
  name: session-claim0
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
