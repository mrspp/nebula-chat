apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: sync-claim0
  name: sync-claim0
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
