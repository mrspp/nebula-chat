apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: auth-key-claim0
  name: auth-key-claim0
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
