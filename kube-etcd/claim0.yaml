apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: etcd-claim0
  name: etcd-claim0
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
