apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: auth-session-claim0
  name: auth-session-claim0
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
