apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: document-claim0
  name: document-claim0
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
